<h2>{{title}}</h2>

<div class="row">
    <div class="col-md-6">
        {{library-item-form item=model action=modelAction buttonLabel=buttonLabel}}
    </div>

    <div class="col-md-4">
        {{library-item item=model}}
    </div>
</div>
